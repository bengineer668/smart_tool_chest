# Requirements

opencv-python-headless>=4.8
numpy>=1.24
Pillow>=9.2

# On Raspberry Pi with PiCamera module, also install:
# picamera2
# (comes pre-installed on Raspberry Pi OS Bookworm)

# TFT display (JC2432S028 / ILI9341) — display_test.py writes directly to
# the kernel framebuffer (/dev/fb0), so no extra Python libs are needed.
# RPi.GPIO is only required for the backlight PWM:
# pip install RPi.GPIO
#
# Add yourself to the video group to avoid running as root:
#   sudo usermod -a -G video $USER   (then log out and back in)
